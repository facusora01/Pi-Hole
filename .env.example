# Pi-hole Environment Configuration Template
# ================================================================
# SECURITY WARNING: This file contains sensitive configuration!
# Copy this file to .env and customize the values for your setup
# NEVER commit the .env file to version control!
# ================================================================

# 🌐 NETWORK CONFIGURATION (REQUIRED)
# Pi-hole Server IP Address - Change this to YOUR server's IP
PIHOLE_SERVER_IP=192.168.1.100

# Port Configuration - Change if default ports are occupied
WEB_PORT=8080                # Web interface port (access via http://YOUR_IP:8080)
DNS_PORT=53                  # DNS service port (devices use this for DNS queries)

# 🔐 SECURITY (REQUIRED)
# Pi-hole Web Interface Password - Use a strong, unique password
# Generate with: Get-Random -Count 16 -InputObject ([char[]]'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*')
PIHOLE_WEBPASSWORD=your-secure-password-here

# 🌍 LOCALIZATION (OPTIONAL)
# Timezone for proper log timestamps
# Find yours: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/Argentina/Buenos_Aires

# 🐳 DOCKER CONFIGURATION (OPTIONAL)
# Container name and image version
CONTAINER_NAME=pihole
PIHOLE_IMAGE=pihole/pihole:latest

# 🌐 DNS UPSTREAM SERVERS (OPTIONAL)
# Servers Pi-hole uses for non-blocked domains
UPSTREAM_DNS1=1.1.1.1        # Cloudflare (fast, privacy-focused)
UPSTREAM_DNS2=8.8.8.8        # Google (reliable backup)

# 🎨 ADVANCED SETTINGS (OPTIONAL)
# Pi-hole domain and web theme
PIHOLE_DOMAIN=pi.hole
WEBTHEME=default-dark        # Options: default-dark, default-light

# DHCP Configuration (optional)
# Uncomment and configure if using Pi-hole as DHCP server
# DHCP_START=192.168.1.100
# DHCP_END=192.168.1.200
# DHCP_ROUTER=192.168.1.1

# Security Note:
# Never commit the actual .env file to version control
# This template shows the required variables without exposing secrets